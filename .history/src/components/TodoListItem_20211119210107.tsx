import React from 'react'

interface TodoListItemProps {
    todos: Todo;
    toggleComplete: ToggleComplete;
}

const TodoListItem = (TodoListItemProps: Props) => {
    return (
        <div>
            
        </div>
    )
}

export default TodoListItem
